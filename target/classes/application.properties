# Database Configuration (MySQL)
# Uses environment variables with sensible defaults for local development
# spring.datasource.url=jdbc:mysql://localhost:3306/medical_prescription_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=__Prince_46
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# src/main/resources/application.properties
spring.datasource.url=jdbc:postgresql://dpg-d622q0q4d50c7384jft0-a:5432/prescription_manager?sslmode=require
spring.datasource.username=prescription_manager_user
spring.datasource.password=NAXaWDDftRQfdvwVesXlpi3iR0CDmdmd
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JPA/Hibernate Configuration
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8082

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.medical.prescription=DEBUG

# Force Hibernate to use PostgreSQL dialect to avoid backtick errors
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


# Ensure Hibernate doesn't use backticks for quoting
spring.jpa.properties.hibernate.globally_quoted_identifiers=false